- name: Shell vs ansible command
  hosts: web
  tasks:
    - name: Printing the users
      ansible.builtin.shell: cat /etc/passwd | grep root
      register: users_output

    - name: Printing the users_output
      ansible.builtin.debug:
        msg: "{{ users_output }}"
